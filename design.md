# 可视化数据集成平台

## 产品需求文档

---

## 一、文档说明

1. 平台形态：类 n8n 可视化拖拽流程编排的数据集成平台
2. 核心约束：**1 个 Flow 只对应 1 个目标平台**
3. 批量拆分目的：**满足目标平台单批次最大条数限制**
4. 推送结果统一通过**推送结果提取器**解析，支持 JSONPath / 脚本
5. 异步模式下，由提取器获取**平台批次 ID**，供后续异步查询使用
6. 本文档只描述功能、行为、规则、状态、指标、展示，不涉及架构、技术选型、代码实现
7. 适用场景：企业多系统→多外部平台数据分发、同步、集成
8. 项目名称 Vortex

---

## 二、产品定位

低代码可视化数据集成平台，提供：

- 可视化拖拽编排，**1 Flow = 1 目标平台**
- 混合触发 + 数据整合器（防丢失、可控频率）
- 表达式引擎统一支撑过滤、条件判断、字段计算
- 数据过滤、转换、按平台限制自动批量拆分
- 统一推送 + 推送结果提取器（JSONPath / 脚本）
- 同步返回 / 异步查询两种结果模式统一管理
- 编码式复杂对接兼容并保持全链路可追踪
- 全节点状态、流入/流出量、吞吐量实时监控
- 单条数据 + 批次双维度可追溯、可重试、可审计

目标：**不丢数据、不重数据、平台隔离、统一解析、统一监控、统一追溯**。

---

## 三、核心流程（单 Flow 结构）

```
多触发源（定时/CDC/HTTP/手动）
        ↓
数据整合器（收集 ID + 去重 + 暂存 + 定时统一推送）
        ↓
根据 ID 拉取全量业务数据
        ↓
转换前过滤（表达式引擎）
        ↓
数据转换（字段映射/脚本/微服务）
        ↓
转换后过滤 + 前置依赖校验（表达式引擎）
        ↓
批量拆分节点（按当前目标平台最大条数限制拆分）
        ↓
单批次推送
        ↓
推送结果提取器（JSONPath / 脚本，同步取结果/异步取批次 ID）
        ↓
【异步模式】异步状态轮询/回调
        ↓
更新数据映射表（内部 ID ↔ 当前平台 ID）
        ↓
全链路日志、状态、指标落地
```

---

## 四、功能模块详细需求

### 4.1 Flow 与平台关系

1. **1 个 Flow 只归属 1 个目标平台**，不支持一对多推送。
2. 如需推送到多个平台，创建多个独立 Flow。
3. 每个 Flow 独立配置：

   - 触发规则
   - 数据源与拉取
   - 过滤与转换
   - 批量拆分策略
   - 推送参数与模式
   - 告警、重试、权限
4. 各 Flow 之间运行、状态、失败、日志**完全隔离**。

---

### 4.2 可视化流程画布（类 n8n）

1. 画布操作

   - 节点拖拽、连线、复制、删除、启用/禁用
   - 撤销、重做、缩放、居中、导航
   - 保存、导入、导出、模板复用
2. 运行时节点染色

   - 未运行：灰色
   - 执行中：黄色
   - 执行成功：绿色
   - 执行失败：红色
   - 已过滤：灰色虚线
   - 阻塞/暂停：橙色
   - 异步处理中：蓝色
3. 点击节点展示：配置、实时指标、执行日志、数据快照。

---

### 4.3 触发节点（多源混合）

支持同一 Flow 绑定多个触发源，统一输出数据 ID 到整合器：

1. 定时触发：Cron、立即执行
2. CDC 实时触发：数据库增删改，仅收集 ID
3. HTTP 触发（Webhook）：外部调用传入 ID
4. 手动触发：输入/导入/勾选 ID 触发

---

### 4.4 数据整合器（防丢失核心）

1. 作用：统一收集、合并、去重、暂存多触发源 ID。
2. 能力

   - 自动去重：同一业务 ID 只保留一条
   - 数据暂存：推送成功前不清理
   - 推送策略：定时统一推送
   - 失败 ID 保留到下一轮自动重试
3. 展示指标

   - 待推送总数、流入、流出、去重数
   - 下次执行时间
   - 状态：收集中 / 待推送 / 推送中 / 完成

---

### 4.5 数据源拉取与聚合

1. 数据源类型

   - 业务数据库（SQL）
   - 手动/Excel/批量导入
   - API、文件、消息队列、自定义
2. 逻辑

   - 按整合器输出的 ID 拉取全量数据
   - 多数据源按主键关联合并
   - 冲突策略：覆盖/忽略/追加/标记异常
3. 指标：流入 ID 数、输出条数、查询耗时、吞吐量。

---

### 4.6 全局表达式引擎（Spring EL）

1. 定位：平台内置标准表达式引擎，统一支撑所有简单逻辑。
2. 技术选型：**Spring Expression Language (SpEL)**
3. 支持

   - 比较：`=`、`!=`、`>`、`>=`、`<`、`<=`
   - 包含、为空、不为空
   - 逻辑：AND、OR、NOT
   - IN、NOT IN
   - 字符串、数值、日期函数
   - 系统变量：当前时间、批次号、业务 ID、平台、重试次数
4. 可视化

   - 表达式编辑器、字段提示、语法校验
   - 测试运行、结果预览
5. 可追溯：每条数据记录表达式内容、执行结果、时间。

---

### 4.7 数据过滤

1. 过滤时机

   - 转换前过滤
   - 转换后过滤
   - 推送前前置依赖校验
2. 过滤方式

   - 简单表达式：表达式引擎执行
   - 脚本过滤：Groovy/JS
   - 依赖校验：表达式判断关联数据状态
3. 追溯：所有过滤记录规则、结果、时间。
4. 指标：流入、流出、过滤条数、过滤率、耗时。

---

### 4.8 数据转换

1. 三种模式

   - 字段映射：可视化拖拽、常量、类型转换、空值处理
   - 脚本转换：自定义脚本处理
   - 委托转换：调用微服务使用返回结果
2. 增强：映射支持**表达式引擎计算**

   - 示例：`price * 1.08`、`if(status=1,"正常","禁用")`
3. 追溯：转换前后快照、异常、耗时。
4. 指标：流入、流出、失败条数、吞吐量。

---

### 4.9 批量拆分节点（按平台限制）

1. 唯一目的：**满足当前 Flow 所属平台单批次最大条数限制**。
2. 配置项

   - 平台单批最大允许条数
   - 批次间发送间隔
   - 失败策略：停止整批 / 跳过失败 / 重试 N 次
3. 行为

   - 自动将大数据集拆分为若干小批次
   - 每批条数 ≤ 平台上限
   - 按顺序串行/配置并发发送
   - 每批次独立状态、独立可重试
4. 指标

   - 总条数、总批次数
   - 当前批次/总批次
   - 每批条数、处理速度、耗时

---

### 4.10 单批次推送

1. 推送模式二选一

   - **同步返回**：接口调用后直接返回最终处理结果
   - **异步查询**：接口仅返回受理成功，需后续凭批次 ID 查询最终结果
2. 推送节点只负责发送请求，**不解析结果**，结果统一由下一节点处理。
3. 通用能力

   - 唯一键：业务 ID + 平台编码
   - 支持重复推送、历史日志保留
   - 支持手动重推：整批/单条/失败重推
4. 指标：流入、成功、失败、异步中条数、耗时、吞吐量。

---

### 4.11 推送结果提取器（核心新增）

1. 位置：紧跟在**单批次推送节点之后**。
2. 职责：从平台返回的响应报文中**提取关键信息**，统一流程上下文。
3. 支持两种提取方式

   1. **JSONPath 提取**

      - 可视化配置 JSONPath 表达式
      - 支持提取：状态、批次 ID、平台 ID、错误信息
   2. **脚本提取**

      - 支持 Groovy/JS 等脚本
      - 适用于复杂结构、加密、非标准返回
4. 按模式提取内容

   - **同步模式**：提取单条/批次状态、平台 ID、错误码、失败原因
   - **异步模式**：**必须提取平台返回的批次 ID/任务 ID**，存入上下文
5. 能力

   - 提取结果存入流程上下文，供后续节点使用
   - 提取失败则标记批次异常
   - 记录原始响应、提取规则、提取结果，可追溯
6. 指标

   - 流入批次数量
   - 提取成功数
   - 提取失败数
   - 执行耗时

---

### 4.12 异步状态查询节点

仅异步平台使用：

1. 使用**推送结果提取器**中获取的**平台批次 ID**进行轮询
2. 可配置：轮询间隔、最大轮询次数、超时时间
3. 自动更新数据状态、映射表、日志
4. 状态流转：已推送 → 异步处理中 → 成功/失败
5. 指标：待更新、成功、失败条数、轮询次数、耗时。

---

### 4.13 编码式对接节点（可追踪）

用于复杂对接：签名、加密、私有协议、SDK 等。

1. 允许手写代码实现自定义逻辑
2. 代码只负责：数据处理 + 外部调用 + 返回标准结果
3. 平台统一接管：状态、日志、重试、批次、映射、监控、追溯
4. 必须返回标准结构

   - 成功/失败
   - 平台 ID/第三方单号
   - 错误信息
5. 全程可追溯：入参、出参、耗时、异常堆栈。

---

### 4.14 数据映射表（内部 ID ↔ 平台 ID）

1. 唯一键：**业务 ID + 平台编码**
2. 存储内容

   - 内部业务 ID
   - 平台返回 UUID/唯一 ID
   - 最近一次推送状态
   - 结构化失败原因
   - 批次号、推送时间
3. 规则

   - 只保留最新一条映射记录
   - 历史推送存入日志，不覆盖
4. 查询：按内部 ID 查平台映射、按平台 ID 反查内部数据。

---

### 4.15 全链路追溯（核心）

1. 单条数据全生命周期可查

   - 来源：触发类型、入整合器时间
   - 过滤、转换、批量、推送全过程
   - 同步/异步状态、映射 ID、失败原因
   - 每一步节点流入/流出、耗时、状态
2. 批次追溯可查

   - 总条数、成功、失败、过滤
   - 执行时间、耗时、配置快照
   - 支持批次内明细下钻到单条数据。

---

### 4.16 全节点实时监控

每个节点强制展示：

1. 节点状态
2. 实时流量

   - 流入条数 in
   - 流出条数 out
   - 过滤条数 filter
   - 失败条数 fail
   - 异步处理中条数 async
3. 性能与吞吐量

   - 执行耗时
   - 处理速度：条/秒
   - 批次信息、并发数

全局任务指标：

- 任务状态
- 总流入、总成功、总失败、总过滤
- 整体吞吐量、总耗时

---

### 4.17 基础配套能力

1. 任务管理：增删改查、启用/禁用、启停、重推
2. 告警：批次失败、连续失败、超时、异常
3. 操作审计：配置修改、手动触发、重推、操作日志
4. 数据安全：敏感字段脱敏、日志可配置是否存明文
5. 权限：按任务、数据源、平台分配权限

---

## 五、典型业务场景（多平台）

```
平台 A Flow：
触发 → 整合器 → 拉取数据 → 过滤 → 转换 → 按 A 上限批量拆分 → 推送 A → 结果提取器 → 映射 A

平台 B Flow：
触发 → 整合器 → 拉取数据 → 过滤 → 转换 → 按 B 上限批量拆分 → 推送 B → 结果提取器 → 异步查询 → 映射 B
```

---

## 六、需求总结

本平台采用**1 Flow = 1 目标平台**架构，数据经混合触发收集、去重、过滤、转换后，**严格按目标平台单批次条数限制自动拆分推送**；
通过**推送结果提取器**统一解析响应（支持 JSONPath / 脚本），异步模式下自动提取批次 ID 用于后续状态查询；
整套流程支持同步/异步两种结果模式、编码式复杂对接、表达式引擎逻辑统一、全节点流量/吞吐量/状态可观测、单条 + 批次双维度可追溯，满足企业级数据集成**稳定、隔离、可控、可查、可运维**的全部要求。
